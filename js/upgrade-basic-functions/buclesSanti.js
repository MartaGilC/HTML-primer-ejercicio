//Bucle que empiece en 30, reste de 2 en 2, y que el límite sea -10
for (let x = 30; x >= -10; x-=2 ) {
    console.log(x) }
    
    //Buble empiece en 100 y vaya dividiendo de dos en dos, hasta que este por debajo de 10;
    for (let i = 100; i >= 9; i = i/= 2)
    {console.log(i)}
    
    //Bucle que empiece en 1.01, que multiplique *2, y que llegue hasta 100
    
    for (let m = 1.01; m <= 100; m *= 2)
    {console.log(m)}

    //Crea una pirámide
let piramide = "hey"
for (let p = 2; p < 15; p++)
{let m = 1; m < p; m++}
{piramide}
console.log(piramide)
   